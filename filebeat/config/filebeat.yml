#filebeat.inputs:
#   - type: log
#     enabled: true
#     paths:
#        - '/var/lib/docker/containers/*.log'

filebeat.modules:
  - module: postgresql
    log:
      enabled: true
      var.paths: ['/var/log/postgresql/*.log']

output.logstash:
   enabled: true
   hosts: ["logstash:5044"]